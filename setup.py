from setuptools import setup, find_packages

setup(
    name='bank_deposit',
    version='0.0.1',
    description='Bank Deposit Integration for ERPNext',
    author='Your Name',
    packages=find_packages(),
    zip_safe=False,
    include_package_data=True,
    install_requires=[]
)